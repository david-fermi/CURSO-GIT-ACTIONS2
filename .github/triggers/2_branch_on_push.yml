name: Github actions - Push Branch Template 
run-name: Running - ${{ github.event_name }}
on:
  push:
    branches:
      - main
      - develop
      - 'release/**'
jobs:
  on-push:
    runs-on: ubuntu-latest
    steps:
      - name: Comments
        run: | 
          echo "Comment on push workflow - branch"